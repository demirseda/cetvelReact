import React, { Component } from 'react';
import './dataSlider.css';


export default function btnSlider(direction,moveSlide) {
    console.log(direction)
  return(
        <button className='btnSlide bx bx-chevron-right'>
        </button>
  );
}
